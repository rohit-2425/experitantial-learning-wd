<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Converge 2026 Speaker Directory</title>
<script src="https://cdn.tailwindcss.com"></script>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  body { font-family: 'Inter', sans-serif; }
  h1, h2, h3 { font-family: 'Space Grotesk', sans-serif; }
</style>
</head>
<body class="bg-[#12131A] text-[#EDEEF3]">

  <div class="max-w-6xl mx-auto">

    <!-- ================= HEADER ================= -->
    <header class="px-6 md:px-12 py-12 md:py-16 border-b border-white/10">
      <h1 class="text-4xl md:text-5xl font-bold leading-tight">Converge 2026<br>Speaker Directory</h1>
      <p class="mt-4 max-w-xl text-[#9CA0B3] leading-relaxed">
        Browse this year's lineup, filter speakers by topic, and see where each session falls across the two-day program.
      </p>

      <div class="mt-10 flex flex-wrap gap-8">
        <div>
          <p class="text-3xl font-bold" id="statSpeakerCount">0</p>
          <p class="text-sm text-[#9CA0B3] mt-1">Speakers</p>
        </div>
        <div class="border-l border-white/10 pl-8">
          <p class="text-3xl font-bold" id="statTrackCount">0</p>
          <p class="text-sm text-[#9CA0B3] mt-1">Topic tracks</p>
        </div>
        <div class="border-l border-white/10 pl-8">
          <p class="text-3xl font-bold" id="statDayCount">0</p>
          <p class="text-sm text-[#9CA0B3] mt-1">Program days</p>
        </div>
      </div>
    </header>

    <!-- ================= SEARCH + FILTER ================= -->
    <section class="px-6 md:px-12 py-8 border-b border-white/10">
      <input
        id="searchInput"
        type="text"
        placeholder="Search speakers by name or role"
        class="w-full max-w-md rounded-md bg-[#1B1D2A] border border-white/10 px-4 py-2 text-sm placeholder-[#9CA0B3] focus:outline-none focus:ring-2 focus:ring-[#E8A33D]"
      >
      <div id="tagFilters" class="flex flex-wrap gap-2 mt-4"></div>
    </section>

    <!-- ================= SPEAKER GRID ================= -->
    <section class="px-6 md:px-12 py-10">
      <h2 class="text-xl font-semibold mb-6">Speakers</h2>
      <div id="speakerGrid" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5"></div>
      <p id="noResults" class="hidden text-[#9CA0B3] mt-6">No speakers match your search.</p>
    </section>

    <!-- ================= SESSION SCHEDULE ================= -->
    <section class="px-6 md:px-12 py-10 border-t border-white/10">
      <h2 class="text-xl font-semibold mb-6">Session schedule</h2>

      <div class="flex gap-2 mb-6">
        <button
          class="day-tab text-sm px-4 py-1.5 rounded-full border bg-transparent text-[#9CA0B3] border-white/15 transition-colors focus:outline-none focus:ring-2 focus:ring-[#E8A33D]"
          data-day="1"
        >Day 1</button>
        <button
          class="day-tab text-sm px-4 py-1.5 rounded-full border bg-transparent text-[#9CA0B3] border-white/15 transition-colors focus:outline-none focus:ring-2 focus:ring-[#E8A33D]"
          data-day="2"
        >Day 2</button>
      </div>

      <div id="scheduleList" class="space-y-3"></div>
    </section>

    <!-- ================= FOOTER ================= -->
    <footer class="px-6 md:px-12 py-8 text-sm text-[#9CA0B3] border-t border-white/10">
      Converge 2026 speaker directory, built for challenge WD-T1-197.
    </footer>

  </div>

  <!-- ================= SPEAKER MODAL ================= -->
  <div id="speakerModal" class="hidden fixed inset-0 z-50 flex items-center justify-center bg-black/60 px-4">
    <div class="bg-[#1B1D2A] border border-white/10 rounded-lg max-w-lg w-full p-6 relative max-h-[85vh] overflow-y-auto">
      <button
        id="closeModalBtn"
        class="absolute top-4 right-4 text-[#9CA0B3] hover:text-white text-xl leading-none focus:outline-none focus:ring-2 focus:ring-[#E8A33D] rounded"
        aria-label="Close"
      >&times;</button>
      <div id="modalContent"></div>
    </div>
  </div>

  <script>
    // ============================================================
    // DATA
    // These arrays are the "database" for the whole page.
    // Every speaker card, tag, and schedule row is built from this.
    // ============================================================

    const speakers = [
      {
        id: 1,
        name: "Priya Nair",
        role: "Senior Frontend Engineer, Nimbus Labs",
        tracks: ["Web Development", "UX Design"],
        shortBio: "Builds design systems used by frontend teams across three continents.",
        bio: "Priya leads the frontend platform team at Nimbus Labs, where she built the component library now used by over forty product teams. She has spent the last two years pairing design and engineering workflows so that neither side has to guess what the other needs."
      },
      {
        id: 2,
        name: "Marcus Chen",
        role: "ML Research Lead, Fathom AI",
        tracks: ["AI & ML"],
        shortBio: "Leads applied research on retrieval and reasoning at Fathom AI.",
        bio: "Marcus runs the applied research group at Fathom AI, focusing on retrieval-augmented reasoning systems. Before that, he spent six years publishing on sequence modeling and now spends most of his time explaining why the results are harder to reproduce than the papers suggest."
      },
      {
        id: 3,
        name: "Fatima Al-Sayed",
        role: "Cloud Architect, Ridgeline Systems",
        tracks: ["Cloud & DevOps"],
        shortBio: "Designs multi-region cloud infrastructure for high-traffic platforms.",
        bio: "Fatima designs the multi-region infrastructure behind several high-traffic consumer platforms. She is especially interested in the unglamorous parts of reliability work: rollback plans, capacity headroom, and the incident reviews nobody wants to write."
      },
      {
        id: 4,
        name: "Diego Ramirez",
        role: "Security Consultant, Wardline Security",
        tracks: ["Cybersecurity"],
        shortBio: "Runs red-team engagements for financial and healthcare clients.",
        bio: "Diego has spent a decade running red-team engagements for financial and healthcare clients, and now trains internal security teams to think like attackers before the attackers do. He believes most breaches start with a bored employee, not a sophisticated exploit."
      },
      {
        id: 5,
        name: "Hannah Kowalski",
        role: "Product Designer, Fieldnote",
        tracks: ["UX Design"],
        shortBio: "Designs field tools for construction and logistics workers.",
        bio: "Hannah designs field software for construction and logistics crews, which means testing prototypes on job sites in the rain rather than in a conference room. Her talks usually come with a story about a design decision that only survived first contact with a real user."
      },
      {
        id: 6,
        name: "Kenji Watanabe",
        role: "Data Scientist, Northbay Analytics",
        tracks: ["Data Science", "AI & ML"],
        shortBio: "Builds forecasting models for retail demand planning.",
        bio: "Kenji builds demand forecasting models used by retail planning teams to decide what to stock months in advance. He is currently more interested in explaining model uncertainty to non-technical stakeholders than in squeezing out another point of accuracy."
      },
      {
        id: 7,
        name: "Aisha Bello",
        role: "DevOps Engineer, Ridgeline Systems",
        tracks: ["Cloud & DevOps", "Cybersecurity"],
        shortBio: "Builds deployment pipelines with security checks built in.",
        bio: "Aisha builds the deployment pipelines at Ridgeline Systems, with security scanning built into every stage rather than bolted on at the end. She is a strong believer that the easiest vulnerability to fix is the one caught before it ships."
      },
      {
        id: 8,
        name: "Sam O'Connor",
        role: "Full-Stack Developer, Freelance",
        tracks: ["Web Development"],
        shortBio: "Freelance developer building tools for independent creators.",
        bio: "Sam is a freelance developer who builds small, focused tools for independent creators, things like invoice generators and simple booking pages. He talks often about the tradeoffs of picking boring, reliable technology over the newest framework."
      }
    ];

    const sessions = [
      { id: 1,  day: 1, time: "9:30 AM",  room: "Hall A",   title: "Design Systems at Scale",                     track: "Web Development", speakerId: 1 },
      { id: 2,  day: 1, time: "9:30 AM",  room: "Studio 1", title: "Retrieval-Augmented Reasoning in Practice",   track: "AI & ML",          speakerId: 2 },
      { id: 3,  day: 1, time: "11:00 AM", room: "Hall B",   title: "Designing for the Job Site",                  track: "UX Design",        speakerId: 5 },
      { id: 4,  day: 1, time: "11:00 AM", room: "Hall A",   title: "Multi-Region Infrastructure Without the Guesswork", track: "Cloud & DevOps", speakerId: 3 },
      { id: 5,  day: 1, time: "2:00 PM",  room: "Studio 2", title: "Forecasting Under Uncertainty",               track: "Data Science",     speakerId: 6 },
      { id: 6,  day: 1, time: "3:30 PM",  room: "Hall B",   title: "How Most Breaches Actually Start",            track: "Cybersecurity",    speakerId: 4 },
      { id: 7,  day: 2, time: "9:30 AM",  room: "Studio 1", title: "Security Built Into the Pipeline",            track: "Cloud & DevOps",   speakerId: 7 },
      { id: 8,  day: 2, time: "9:30 AM",  room: "Hall A",   title: "Boring Technology, Reliable Products",        track: "Web Development", speakerId: 8 },
      { id: 9,  day: 2, time: "11:00 AM", room: "Hall B",   title: "What Design Systems Get Wrong",               track: "UX Design",        speakerId: 1 },
      { id: 10, day: 2, time: "11:00 AM", room: "Studio 2", title: "Explaining Models to Non-Technical Teams",    track: "AI & ML",          speakerId: 6 },
      { id: 11, day: 2, time: "2:00 PM",  room: "Hall A",   title: "Capacity Planning Before You Need It",        track: "Cloud & DevOps",   speakerId: 3 },
      { id: 12, day: 2, time: "3:30 PM",  room: "Studio 1", title: "Where Reasoning Models Still Fail",           track: "AI & ML",          speakerId: 2 }
    ];

    const trackColors = {
      "Web Development": "#2DD4BF",
      "AI & ML": "#A78BFA",
      "Cloud & DevOps": "#FB923C",
      "UX Design": "#F472B6",
      "Cybersecurity": "#F87171",
      "Data Science": "#38BDF8"
    };

    // ============================================================
    // STATE
    // Plain variables that track what the user has currently selected.
    // ============================================================

    let currentTag = "All";
    let currentSearch = "";
    let currentDay = 1;

    // ============================================================
    // HELPER FUNCTIONS
    // ============================================================

    function getInitials(name) {
      const parts = name.split(" ");
      const first = parts[0] ? parts[0][0] : "";
      const last = parts.length > 1 ? parts[parts.length - 1][0] : "";
      return (first + last).toUpperCase();
    }

    function getTrackColor(track) {
      return trackColors[track] || "#9CA0B3";
    }

    // Collects every unique track name across all speakers, in order.
    function getAllTracks() {
      const tracks = [];
      speakers.forEach(function (speaker) {
        speaker.tracks.forEach(function (track) {
          if (!tracks.includes(track)) {
            tracks.push(track);
          }
        });
      });
      return tracks;
    }

    function tagPillHTML(track) {
      const color = getTrackColor(track);
      return '<span class="text-xs font-medium px-2 py-1 rounded-full" style="background-color:' + color + '26; color:' + color + '">' + track + '</span>';
    }

    // ============================================================
    // RENDER FUNCTIONS
    // Each one rebuilds a piece of the page from the data + state above.
    // ============================================================

    function renderStats() {
      document.getElementById("statSpeakerCount").textContent = speakers.length;
      document.getElementById("statTrackCount").textContent = getAllTracks().length;

      const days = [];
      sessions.forEach(function (session) {
        if (!days.includes(session.day)) {
          days.push(session.day);
        }
      });
      document.getElementById("statDayCount").textContent = days.length;
    }

    function renderTagFilters() {
      const container = document.getElementById("tagFilters");
      const tracks = ["All"].concat(getAllTracks());

      let html = "";
      tracks.forEach(function (track) {
        const isActive = track === currentTag;
        const base = "tag-filter-btn text-sm px-3 py-1.5 rounded-full border transition-colors focus:outline-none focus:ring-2 focus:ring-[#E8A33D] ";
        const activeClasses = "bg-[#E8A33D] border-[#E8A33D] text-[#12131A]";
        const inactiveClasses = "bg-transparent border-white/15 text-[#9CA0B3] hover:border-white/30";
        html += '<button class="' + base + (isActive ? activeClasses : inactiveClasses) + '" data-track="' + track + '">' + track + '</button>';
      });

      container.innerHTML = html;
    }

    function speakerCardHTML(speaker) {
      const primaryColor = getTrackColor(speaker.tracks[0]);
      const tagsHTML = speaker.tracks.map(tagPillHTML).join(" ");

      return (
        '<div class="speaker-card cursor-pointer rounded-lg bg-[#1B1D2A] border border-white/10 p-5 hover:border-white/25 transition-colors" data-id="' + speaker.id + '">' +
          '<div class="flex items-center gap-4">' +
            '<div class="w-12 h-12 shrink-0 rounded-full flex items-center justify-center font-semibold text-[#12131A]" style="background-color:' + primaryColor + '">' + getInitials(speaker.name) + '</div>' +
            '<div>' +
              '<h3 class="font-semibold">' + speaker.name + '</h3>' +
              '<p class="text-sm text-[#9CA0B3]">' + speaker.role + '</p>' +
            '</div>' +
          '</div>' +
          '<p class="text-sm text-[#9CA0B3] mt-4">' + speaker.shortBio + '</p>' +
          '<div class="flex flex-wrap gap-2 mt-4">' + tagsHTML + '</div>' +
        '</div>'
      );
    }

    function renderSpeakers() {
      const grid = document.getElementById("speakerGrid");
      const noResults = document.getElementById("noResults");

      const filtered = speakers.filter(function (speaker) {
        const matchesTag = currentTag === "All" || speaker.tracks.includes(currentTag);
        const searchText = (speaker.name + " " + speaker.role).toLowerCase();
        const matchesSearch = searchText.includes(currentSearch.toLowerCase());
        return matchesTag && matchesSearch;
      });

      if (filtered.length === 0) {
        grid.innerHTML = "";
        noResults.classList.remove("hidden");
      } else {
        noResults.classList.add("hidden");
        grid.innerHTML = filtered.map(speakerCardHTML).join("");
      }
    }

    function sessionRowHTML(session) {
      const speaker = speakers.find(function (s) { return s.id === session.speakerId; });
      const color = getTrackColor(session.track);

      return (
        '<div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 rounded-lg bg-[#1B1D2A] border border-white/10 p-4">' +
          '<div>' +
            '<p class="text-sm text-[#9CA0B3]">' + session.time + ' in ' + session.room + '</p>' +
            '<h3 class="font-semibold mt-1">' + session.title + '</h3>' +
            '<button class="speaker-link text-sm mt-1 focus:outline-none focus:underline" style="color:' + color + '" data-id="' + speaker.id + '">' + speaker.name + '</button>' +
          '</div>' +
          '<span class="text-xs font-medium px-2 py-1 rounded-full self-start sm:self-center" style="background-color:' + color + '26; color:' + color + '">' + session.track + '</span>' +
        '</div>'
      );
    }

    function renderSchedule() {
      const list = document.getElementById("scheduleList");
      const daySessions = sessions.filter(function (session) { return session.day === currentDay; });
      list.innerHTML = daySessions.map(sessionRowHTML).join("");
    }

    function renderDayTabs() {
      document.querySelectorAll(".day-tab").forEach(function (btn) {
        const day = Number(btn.dataset.day);
        if (day === currentDay) {
          btn.classList.add("bg-[#E8A33D]", "text-[#12131A]", "border-[#E8A33D]");
          btn.classList.remove("bg-transparent", "text-[#9CA0B3]", "border-white/15");
        } else {
          btn.classList.remove("bg-[#E8A33D]", "text-[#12131A]", "border-[#E8A33D]");
          btn.classList.add("bg-transparent", "text-[#9CA0B3]", "border-white/15");
        }
      });
    }

    // ============================================================
    // MODAL (speaker detail + their sessions)
    // ============================================================

    function openSpeakerModal(id) {
      const speaker = speakers.find(function (s) { return s.id === id; });
      if (!speaker) return;

      const mySessions = sessions.filter(function (s) { return s.speakerId === id; });
      const primaryColor = getTrackColor(speaker.tracks[0]);

      let sessionsHTML = "";
      if (mySessions.length === 0) {
        sessionsHTML = '<p class="text-sm text-[#9CA0B3] mt-2">No sessions scheduled yet.</p>';
      } else {
        sessionsHTML = '<ul class="space-y-2 mt-2">' + mySessions.map(function (s) {
          return '<li class="text-sm text-[#9CA0B3]">Day ' + s.day + ', ' + s.time + ' — ' + s.title + ' (' + s.room + ')</li>';
        }).join("") + '</ul>';
      }

      document.getElementById("modalContent").innerHTML =
        '<div class="flex items-center gap-4">' +
          '<div class="w-14 h-14 shrink-0 rounded-full flex items-center justify-center font-semibold text-lg text-[#12131A]" style="background-color:' + primaryColor + '">' + getInitials(speaker.name) + '</div>' +
          '<div>' +
            '<h3 class="text-lg font-semibold">' + speaker.name + '</h3>' +
            '<p class="text-sm text-[#9CA0B3]">' + speaker.role + '</p>' +
          '</div>' +
        '</div>' +
        '<div class="flex flex-wrap gap-2 mt-4">' + speaker.tracks.map(tagPillHTML).join(" ") + '</div>' +
        '<p class="text-sm text-[#EDEEF3] mt-4 leading-relaxed">' + speaker.bio + '</p>' +
        '<h4 class="text-sm font-semibold mt-6 text-[#9CA0B3]">Sessions</h4>' +
        sessionsHTML;

      document.getElementById("speakerModal").classList.remove("hidden");
    }

    function closeSpeakerModal() {
      document.getElementById("speakerModal").classList.add("hidden");
    }

    // ============================================================
    // EVENT LISTENERS
    // ============================================================

    document.getElementById("searchInput").addEventListener("input", function (e) {
      currentSearch = e.target.value;
      renderSpeakers();
    });

    // Event delegation: one listener on the container instead of one per button.
    document.getElementById("tagFilters").addEventListener("click", function (e) {
      const btn = e.target.closest(".tag-filter-btn");
      if (!btn) return;
      currentTag = btn.dataset.track;
      renderTagFilters();
      renderSpeakers();
    });

    document.getElementById("speakerGrid").addEventListener("click", function (e) {
      const card = e.target.closest(".speaker-card");
      if (!card) return;
      openSpeakerModal(Number(card.dataset.id));
    });

    document.getElementById("scheduleList").addEventListener("click", function (e) {
      const link = e.target.closest(".speaker-link");
      if (!link) return;
      openSpeakerModal(Number(link.dataset.id));
    });

    document.querySelectorAll(".day-tab").forEach(function (btn) {
      btn.addEventListener("click", function () {
        currentDay = Number(btn.dataset.day);
        renderDayTabs();
        renderSchedule();
      });
    });

    document.getElementById("closeModalBtn").addEventListener("click", closeSpeakerModal);

    // Close the modal when clicking the dark backdrop, not the card itself.
    document.getElementById("speakerModal").addEventListener("click", function (e) {
      if (e.target.id === "speakerModal") {
        closeSpeakerModal();
      }
    });

    document.addEventListener("keydown", function (e) {
      if (e.key === "Escape") {
        closeSpeakerModal();
      }
    });

    // ============================================================
    // INITIAL RENDER
    // ============================================================

    renderStats();
    renderTagFilters();
    renderSpeakers();
    renderDayTabs();
    renderSchedule();
  </script>

</body>
</html>
